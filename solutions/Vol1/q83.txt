static means the variable is created only once in the Data Segment and retained even after a function collapses
It is created only once during compile time

count value is retaied across function call

static means these acts like states tht persist in a register

OUTPUT:
11
21
31