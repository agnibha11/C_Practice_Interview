size_t my_strlen(const char *s)
{
    const char *p = s;
    while (*p) p++;
C for Digital Design Interviews  —  page 10

    return (size_t)(p - s);      /* pointer difference = element count */
}
void *my_memcpy(void *dst, const void *src, size_t n)
{
    unsigned char *d = dst;
    const unsigned char *s = src;
    while (n--) *d++ = *s++;
}